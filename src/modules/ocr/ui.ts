/**
 * UI entry point for the OCR module.
 *
 * Kept separate from `index.ts` so application/domain imports stay free of React.
 */

export { OcrReviewPanel } from './ui/ocr-review-panel';
export type { OcrReviewPanelProps } from './ui/ocr-review-panel';
