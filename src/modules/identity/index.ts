/** Public API of the identity module (doc 76 §3). */
export {
  ensureProfile,
  findProfile,
  updateProfile,
  setActiveOrganizationPreference,
  getActiveOrganizationPreference,
} from './application/ensure-profile';
